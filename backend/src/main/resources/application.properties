spring.application.name=EventBookingBackend

# Database Configuration (Supabase PostgreSQL)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.dlpxciimpvqugavnitne}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:RAMAZENDRUM}
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP settings for robust connection management
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.maximum-pool-size=15
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=180000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.keepalive-time=30000
spring.datasource.hikari.auto-commit=true

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.open-in-view=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.batch_size=25
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.fetch_size=100

# Database Configuration (H2 In-Memory for Local Testing) - DISABLED
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true

# Mail Configuration (Brevo/Sendinblue SMTP)
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=a105fc001@smtp-brevo.com
spring.mail.password=${SPRING_MAIL_PASSWORD:your_brevo_api_key_here}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.debug=true



# Server Port (Dynamic for Railway)
server.port=${PORT:8085}
server.forward-headers-strategy=native

# CORS Configuration (Allow Frontend)
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:5175,http://127.0.0.1:3000,http://127.0.0.1:5173,http://127.0.0.1:5174,https://zendrumbooking.vercel.app,https://*.vercel.app

# DEBUG LOGGING (Critical for Security Diagnosis)
# Optimized Logging for Production
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web=INFO
logging.level.com.eventbooking=INFO

# Razorpay Configuration (Test Mode)
razorpay.key.id=rzp_test_placeholder
razorpay.key.secret=placeholder_secret

# Socket.IO Configuration
socketio.host=localhost
socketio.port=8085

# Wallet Service Configuration
# IMPORTANT: Set WALLET_API_KEY in Railway environment variables
wallet.api.key=${WALLET_API_KEY}
wallet.service.url=${WALLET_SERVICE_URL:http://localhost:5000}
wallet.merchant.id=${WALLET_MERCHANT_ID:4f756c24-142b-4143-a957-f8fc5871966a}

# Frontend URL for payment callbacks
app.frontend.url=${APP_FRONTEND_URL:http://localhost:5173}
