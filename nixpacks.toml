[phases.setup]
nixPkgs = ["maven", "jdk17"]

[phases.build]
cmds = ["cd backend && mvn clean package -DskipTests"]

[start]
cmd = "java -Dserver.port=$PORT -jar backend/target/EventBookingBackend.jar"
